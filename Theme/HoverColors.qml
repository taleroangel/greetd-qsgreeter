import QtQuick

/**
 * HoverColors.qml
 * Colors being used by elements that react to hovering and pressing
 */
QtObject {
	property color inactive: Theme.colors.surfaceBright
	property color hover: Theme.colors.primary
	property color pressed: Theme.colors.primaryContrast
}
